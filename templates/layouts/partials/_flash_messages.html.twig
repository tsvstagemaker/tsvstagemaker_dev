 {% for type, messages in app.flashes(['success', 'error', 'info', 'primary', 'danger']) %}
            {% for message in messages %}
                <div class="alert alert-{{ type }} mt-6" role="alert">
                    {{ message }}
                </div>
            {% endfor %}
        {% endfor %}